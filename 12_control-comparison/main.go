package main

import "fmt"

func main() {

	/* 	x, y := 3, "b" // ascii => 98
	   	fmt.Printf("%T, %v\n", x == y, x == y)
	   	fmt.Printf("%T, %v\n", x != y, x != y)
	   	fmt.Printf("%T, %v\n", x < y, x < y)
	   	fmt.Printf("%T, %v\n", x > y, x > y)
	   	fmt.Printf("%T, %v\n", x >= y, x >= y)
		   fmt.Printf("%T, %v\n", x <= y, x <= y) */

	//x, y := 25, 30

	//set1 := (x == y) // false
	//set2 := (x > y) // false

	set3 := true

	/* 	fmt.Printf("%T, %v\n", set1, set1)
	   	fmt.Printf("%T, %v\n", set2, set2) */

	//	fmt.Printf("%v\n", (set1 && set2))
	//fmt.Printf("%v\n", (set3 || set2))

	fmt.Printf("%v\n", (!set3))

}
